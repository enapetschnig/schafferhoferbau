project_id = "fxsjhdsitwtjasxbmksr"

[functions.compare-documents]
verify_jwt = false

[functions.extract-document]
verify_jwt = false

[functions.send-invitation]
verify_jwt = false

# Function prueft Auth selbst (Bearer-Token + Admin-Rollen-Check).
# verify_jwt=true wuerde den Bearer-Token vorab auf Echtheit pruefen — das
# Admin-Role-Check muss die Function trotzdem selbst machen.
[functions.create-employee-account]
verify_jwt = true

# Wird vom CRM ohne Supabase-Schluessel aufgerufen; die Funktion prueft
# stattdessen selbst das gemeinsame Geheimnis (x-cockpit-secret).
[functions.wunsch-datei]
verify_jwt = false
